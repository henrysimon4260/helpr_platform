import { ServiceRequestScreen } from '../../../components/services/composer';
import { wallMountingConfig } from './wall-mounting.config';

export default function WallMounting() {
  return <ServiceRequestScreen config={wallMountingConfig} />;
}
