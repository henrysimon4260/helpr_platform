import { ServiceRequestScreen } from '../../../components/services/composer';
import { movingConfig } from './moving.config';

export default function Moving() {
  return <ServiceRequestScreen config={movingConfig} />;
}
